import * as anchor from '@coral-xyz/anchor';
import { Limitless } from './limitless';

export interface MarketState {
    vaultsCreated: boolean,
    baseMintAddress: anchor.web3.PublicKey,
    baseMintTokenAddress: anchor.web3.PublicKey,
    quoteMintAddress: anchor.web3.PublicKey,
    quoteMintTokenAddress: anchor.web3.PublicKey,
    quoteMintFloorTokenAddress: anchor.web3.PublicKey,
    platformFeeVaultAddress: anchor.web3.PublicKey,
    maxCqd: anchor.BN,
    cqd: anchor.BN,
    offset: anchor.BN,
    minSize: anchor.BN,
    gradient: anchor.BN,
    price: anchor.BN,
    baseDecimals: number,
    quoteDecimals: number,
    floorPrice: anchor.BN,
    highestFloorQuantity: anchor.BN,
    totalMoved: anchor.BN,
    floorPoolSize: anchor.BN,
    basePoolSize: anchor.BN,
    quotePoolSize: anchor.BN,
    startQ: anchor.BN,
    totalSoldBase: anchor.BN,
    totalSoldQuote: anchor.BN,
    totalBoughtBase: anchor.BN,
    totalBoughtQuote: anchor.BN,
    totalSells: anchor.BN,
    totalBuys: anchor.BN,
    totalMinted: anchor.BN,
    totalBurned: anchor.BN,
    totalBurnCost: anchor.BN,
    id: number[],
    creator: anchor.web3.PublicKey,
    launchDate: anchor.BN,
    preMint: anchor.BN,
    preMintClaimed: boolean,
    continuousMint: boolean,
    creatorMinted: anchor.BN,
    buyFee: number,
    sellFee: number,
    receiveAddress: anchor.web3.PublicKey,
    creatorSplit: number,
    backup_set: boolean,
    backup_wallet: anchor.web3.PublicKey,
    new_creator: anchor.web3.PublicKey,
    proposalActive: boolean,
    index: anchor.BN,
    scalerDecimals: number,
    divisorPow: number,
    totalDeposit: anchor.BN,
    totalBorrow: anchor.BN,
    baseDepositAddress: anchor.web3.PublicKey,
    presaleDateOffset: anchor.BN,
    presaleBase: anchor.BN,
    presaleQuote: anchor.BN,
    pow1: number,
    pow2: number
}